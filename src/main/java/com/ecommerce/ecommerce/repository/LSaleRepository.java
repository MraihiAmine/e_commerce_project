package com.ecommerce.ecommerce.repository;

import com.ecommerce.ecommerce.entity.LSale;
import org.springframework.data.jpa.repository.JpaRepository;

public interface LSaleRepository extends JpaRepository<LSale, Long > {
}
